/** @type {import("prettier").Config} */
const config = {
  endOfLine: "auto",
  printWidth: 100,
  proseWrap: "preserve",
  semi: true,
  tabWidth: 2,
  trailingComma: "all",
  useTabs: false,
};

export default config;
